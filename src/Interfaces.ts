export interface IUser {
  blobImageUser?: string;
  strNames?: string;
  strUser?: string;
  strPassword?: string;
}

export interface IUserData {
  blobImageUser: string;
  strNameAndLastName: String;
  strUser?: string;
  stPassword?: string;
}
